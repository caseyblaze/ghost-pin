# Ghost-Pin Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打造一個改變實體 iPhone 定位的介面 — Expo App 輸入座標，Node.js Server 透過 pymobiledevice3 改變 iOS 17+ 裝置的 GPS，並提供一鍵恢復。

**Architecture:** 三層架構。Expo App 純 UI，透過 HTTP 呼叫 Node.js Server。Server 用 Express 提供三個端點（`/location`、`/reset`、`/status`），驗證座標後以子行程呼叫 `pymobiledevice3 developer dvt simulate-location`。pymobiledevice3 透過使用者手動常駐的 `tunneld` 與 iPhone 溝通；Server 絕不執行 sudo。

**Tech Stack:** Node.js 22 + Express + Jest（Server）；Expo（React Native）+ TypeScript（App）；pymobiledevice3（CLI，外部依賴）。

---

## File Structure

```
ghost-pin/
├── presets.json                      # 共用預設地點清單（座標來源）
├── server/
│   ├── package.json
│   ├── src/
│   │   ├── index.js                  # Express app 啟動、掛載路由
│   │   ├── validate.js               # 座標數值/範圍驗證（純函式）
│   │   ├── pmd.js                     # pymobiledevice3 子行程封裝（set/clear/status）
│   │   └── routes.js                 # /location /reset /status 路由 → validate + pmd
│   └── tests/
│       ├── validate.test.js
│       ├── pmd.test.js               # 以 mock child_process 測指令組裝與錯誤包裝
│       └── routes.test.js            # 以 supertest + mock pmd 測 HTTP 行為
└── app/                              # Expo 專案（expo init 產生後增修）
    ├── App.tsx                       # 主畫面：輸入框、預設清單、動作鈕、狀態列
    ├── src/
    │   ├── api.ts                    # 呼叫 Server 的 HTTP client
    │   └── config.ts                 # Server base URL（預設 http://localhost:3000）
    └── ...（Expo 預設檔案）
```

**職責邊界：**
- `validate.js` 只做純資料驗證，零 I/O，最好測。
- `pmd.js` 是唯一碰 `child_process` 的地方，把 CLI 細節隔離起來。
- `routes.js` 串接 validate + pmd，回統一 JSON 格式。
- App 端 `api.ts` 隔離所有 HTTP，`App.tsx` 只管 UI 狀態。

---

## Phase 0 — Spike（手動驗證，無程式碼）

> **目的：** 在實機跑通一次完整鏈路，確認硬體可行再投入開發。**全部通過才進 Phase 1。** 此階段不寫程式、不 commit。

- [ ] **Step 1: 安裝 pymobiledevice3**

Run: `pipx install pymobiledevice3` （若無 pipx：`brew install pipx && pipx ensurepath`，或 `pip3 install pymobiledevice3`）
Expected: `pymobiledevice3 version` 印出版本號（≥ 4.x）

- [ ] **Step 2: iPhone 開 Developer Mode 並信任電腦**

USB 連接 iPhone → Mac。iPhone：設定 > 隱私權與安全性 > 開發者模式 → 開啟 → 重開機 → 信任此電腦。
驗證：`pymobiledevice3 usbmux list` 應列出該裝置（含 UDID）。

- [ ] **Step 3: 掛載 DeveloperDiskImage**

Run: `pymobiledevice3 mounter auto-mount`
Expected: 印出掛載成功，或 "DeveloperDiskImage already mounted"。

- [ ] **Step 4: 啟動常駐 tunnel（保持此終端機開著）**

Run: `sudo pymobiledevice3 remote tunneld`
Expected: 持續運行、印出 tunnel 服務啟動訊息。**不要關閉這個視窗。**

- [ ] **Step 5: 設定模擬座標（另開終端機）**

Run: `pymobiledevice3 developer dvt simulate-location set --tunnel '' -- 25.0330 121.5654`
Expected: 指令成功結束，無 error。

- [ ] **Step 6: 在 iPhone 地圖 App 確認**

打開 iPhone「地圖」App，點定位按鈕。
**驗收：** 藍點出現在台北101（25.0330, 121.5654）。

- [ ] **Step 7: 清除模擬，恢復真實定位**

Run: `pymobiledevice3 developer dvt simulate-location clear --tunnel ''`
**驗收：** iPhone 地圖藍點回到真實位置。

> **若卡住（特別是 iOS 18）：** 參考 pymobiledevice3 issue #572 / #1217。常見處理：iPhone 設定 > 開發者 清除信任的電腦後重新配對；確認 DDI 版本符合 iOS 版本。除錯通過後再繼續。

---

## Phase 1 — Node.js Server

### Task 1: 初始化 Server 專案與 presets

**Files:**
- Create: `presets.json`
- Create: `server/package.json`
- Create: `server/.gitignore`

- [ ] **Step 1: 建立共用預設地點清單**

Create `presets.json`:

```json
[
  { "name": "台北101", "lat": 25.0330, "lng": 121.5654 },
  { "name": "台北車站", "lat": 25.0478, "lng": 121.5170 },
  { "name": "桃園機場", "lat": 25.0797, "lng": 121.2342 }
]
```

- [ ] **Step 2: 建立 server/package.json**

Create `server/package.json`:

```json
{
  "name": "ghost-pin-server",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
```

- [ ] **Step 3: 建立 server/.gitignore**

Create `server/.gitignore`:

```
node_modules/
```

- [ ] **Step 4: 安裝依賴**

Run: `cd server && npm install`
Expected: `node_modules/` 建立，無 error。

- [ ] **Step 5: Commit**

```bash
git add presets.json server/package.json server/package-lock.json server/.gitignore
git commit -m "chore: init server project and presets"
```

---

### Task 2: 座標驗證（validate.js）

**Files:**
- Create: `server/src/validate.js`
- Test: `server/tests/validate.test.js`

- [ ] **Step 1: 寫失敗測試**

Create `server/tests/validate.test.js`:

```javascript
const { validateCoords } = require('../src/validate');

describe('validateCoords', () => {
  test('accepts valid coordinates', () => {
    expect(validateCoords(25.0330, 121.5654)).toEqual({ ok: true, lat: 25.0330, lng: 121.5654 });
  });

  test('accepts numeric strings and coerces them', () => {
    expect(validateCoords('25.0330', '121.5654')).toEqual({ ok: true, lat: 25.0330, lng: 121.5654 });
  });

  test('accepts boundary values', () => {
    expect(validateCoords(-90, -180).ok).toBe(true);
    expect(validateCoords(90, 180).ok).toBe(true);
  });

  test('rejects lat out of range', () => {
    expect(validateCoords(91, 0)).toEqual({ ok: false, message: 'lat 超出範圍 [-90, 90]' });
  });

  test('rejects lng out of range', () => {
    expect(validateCoords(0, 181)).toEqual({ ok: false, message: 'lng 超出範圍 [-180, 180]' });
  });

  test('rejects non-numeric input', () => {
    expect(validateCoords('abc', 0)).toEqual({ ok: false, message: 'lat 不是有效數字' });
    expect(validateCoords(0, 'xyz')).toEqual({ ok: false, message: 'lng 不是有效數字' });
  });

  test('rejects missing input', () => {
    expect(validateCoords(undefined, undefined)).toEqual({ ok: false, message: 'lat 不是有效數字' });
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd server && npx jest tests/validate.test.js`
Expected: FAIL — "Cannot find module '../src/validate'"

- [ ] **Step 3: 寫最小實作**

Create `server/src/validate.js`:

```javascript
function validateCoords(latInput, lngInput) {
  const lat = Number(latInput);
  if (latInput === undefined || latInput === null || latInput === '' || Number.isNaN(lat)) {
    return { ok: false, message: 'lat 不是有效數字' };
  }
  const lng = Number(lngInput);
  if (lngInput === undefined || lngInput === null || lngInput === '' || Number.isNaN(lng)) {
    return { ok: false, message: 'lng 不是有效數字' };
  }
  if (lat < -90 || lat > 90) {
    return { ok: false, message: 'lat 超出範圍 [-90, 90]' };
  }
  if (lng < -180 || lng > 180) {
    return { ok: false, message: 'lng 超出範圍 [-180, 180]' };
  }
  return { ok: true, lat, lng };
}

module.exports = { validateCoords };
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd server && npx jest tests/validate.test.js`
Expected: PASS（7 個測試全綠）

- [ ] **Step 5: Commit**

```bash
git add server/src/validate.js server/tests/validate.test.js
git commit -m "feat: add coordinate validation"
```

---

### Task 3: pymobiledevice3 封裝（pmd.js）

**Files:**
- Create: `server/src/pmd.js`
- Test: `server/tests/pmd.test.js`

說明：`pmd.js` 用 `execFile`（非 `exec`）以參數陣列傳遞座標，避免 shell 注入。匯出 `setLocation`、`clearLocation`、`getStatus`，皆回傳 Promise。測試以注入假的 `execFile` 函式來驗證指令組裝與錯誤包裝，不真的呼叫 CLI。

- [ ] **Step 1: 寫失敗測試**

Create `server/tests/pmd.test.js`:

```javascript
const { createPmd } = require('../src/pmd');

function fakeExecFile(result) {
  // 記錄呼叫並依 result 回呼
  const calls = [];
  const fn = (file, args, cb) => {
    calls.push({ file, args });
    if (result.error) cb(result.error, '', result.stderr || '');
    else cb(null, result.stdout || '', '');
  };
  fn.calls = calls;
  return fn;
}

describe('pmd', () => {
  test('setLocation builds dvt set command with coords as separate args', async () => {
    const exec = fakeExecFile({ stdout: 'ok' });
    const pmd = createPmd(exec);
    const res = await pmd.setLocation(25.0330, 121.5654);
    expect(res).toEqual({ ok: true, message: 'ok' });
    expect(exec.calls[0].file).toBe('pymobiledevice3');
    expect(exec.calls[0].args).toEqual([
      'developer', 'dvt', 'simulate-location', 'set', '--tunnel', '', '--', '25.033', '121.5654'
    ]);
  });

  test('clearLocation builds dvt clear command', async () => {
    const exec = fakeExecFile({ stdout: 'cleared' });
    const pmd = createPmd(exec);
    const res = await pmd.clearLocation();
    expect(res).toEqual({ ok: true, message: 'cleared' });
    expect(exec.calls[0].args).toEqual([
      'developer', 'dvt', 'simulate-location', 'clear', '--tunnel', ''
    ]);
  });

  test('getStatus returns online when usbmux lists a device', async () => {
    const exec = fakeExecFile({ stdout: '[{"Identifier": "abc123"}]' });
    const pmd = createPmd(exec);
    const res = await pmd.getStatus();
    expect(res).toEqual({ ok: true, online: true, message: '裝置已連線' });
    expect(exec.calls[0].args).toEqual(['usbmux', 'list']);
  });

  test('getStatus returns offline when usbmux lists nothing', async () => {
    const exec = fakeExecFile({ stdout: '[]' });
    const pmd = createPmd(exec);
    const res = await pmd.getStatus();
    expect(res).toEqual({ ok: true, online: false, message: '無裝置連線' });
  });

  test('setLocation wraps CLI failure with stderr summary', async () => {
    const exec = fakeExecFile({ error: new Error('exit 1'), stderr: 'Tunnel not found' });
    const pmd = createPmd(exec);
    const res = await pmd.setLocation(0, 0);
    expect(res.ok).toBe(false);
    expect(res.message).toContain('Tunnel not found');
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd server && npx jest tests/pmd.test.js`
Expected: FAIL — "Cannot find module '../src/pmd'"

- [ ] **Step 3: 寫最小實作**

Create `server/src/pmd.js`:

```javascript
const { execFile: nodeExecFile } = require('child_process');

const BIN = 'pymobiledevice3';

// 接受注入的 execFile 以利測試；預設用 node 的 execFile
function createPmd(execFile = nodeExecFile) {
  function run(args) {
    return new Promise((resolve) => {
      execFile(BIN, args, (error, stdout, stderr) => {
        if (error) {
          const detail = (stderr && stderr.trim()) || error.message;
          resolve({ ok: false, message: `pymobiledevice3 失敗: ${detail}` });
          return;
        }
        resolve({ ok: true, message: (stdout || '').trim() });
      });
    });
  }

  function setLocation(lat, lng) {
    return run([
      'developer', 'dvt', 'simulate-location', 'set', '--tunnel', '',
      '--', String(lat), String(lng),
    ]);
  }

  function clearLocation() {
    return run(['developer', 'dvt', 'simulate-location', 'clear', '--tunnel', '']);
  }

  function getStatus() {
    return new Promise((resolve) => {
      execFile(BIN, ['usbmux', 'list'], (error, stdout) => {
        if (error) {
          resolve({ ok: false, online: false, message: '無法查詢裝置狀態' });
          return;
        }
        let online = false;
        try {
          const list = JSON.parse(stdout || '[]');
          online = Array.isArray(list) && list.length > 0;
        } catch (_) {
          online = false;
        }
        resolve({ ok: true, online, message: online ? '裝置已連線' : '無裝置連線' });
      });
    });
  }

  return { setLocation, clearLocation, getStatus };
}

module.exports = { createPmd };
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd server && npx jest tests/pmd.test.js`
Expected: PASS（5 個測試全綠）

> 注意：測試中座標 `25.0330` 經 `String(Number(...))` 會變 `'25.033'`（尾零省略），測試已預期此值。

- [ ] **Step 5: Commit**

```bash
git add server/src/pmd.js server/tests/pmd.test.js
git commit -m "feat: add pymobiledevice3 wrapper"
```

---

### Task 4: HTTP 路由（routes.js）

**Files:**
- Create: `server/src/routes.js`
- Test: `server/tests/routes.test.js`

說明：`createRoutes(pmd)` 回傳一個 Express Router，注入 `pmd` 以利測試。回應格式統一 `{ ok, message, data? }`。

- [ ] **Step 1: 寫失敗測試**

Create `server/tests/routes.test.js`:

```javascript
const express = require('express');
const request = require('supertest');
const { createRoutes } = require('../src/routes');

function makeApp(pmd) {
  const app = express();
  app.use(express.json());
  app.use(createRoutes(pmd));
  return app;
}

describe('routes', () => {
  test('POST /location with valid coords calls pmd.setLocation and returns ok', async () => {
    const pmd = {
      setLocation: jest.fn().mockResolvedValue({ ok: true, message: 'ok' }),
    };
    const res = await request(makeApp(pmd)).post('/location').send({ lat: 25.033, lng: 121.5654 });
    expect(res.status).toBe(200);
    expect(res.body.ok).toBe(true);
    expect(pmd.setLocation).toHaveBeenCalledWith(25.033, 121.5654);
  });

  test('POST /location with invalid coords returns 400 and does not call pmd', async () => {
    const pmd = { setLocation: jest.fn() };
    const res = await request(makeApp(pmd)).post('/location').send({ lat: 999, lng: 0 });
    expect(res.status).toBe(400);
    expect(res.body.ok).toBe(false);
    expect(pmd.setLocation).not.toHaveBeenCalled();
  });

  test('POST /location returns 500 when pmd fails', async () => {
    const pmd = {
      setLocation: jest.fn().mockResolvedValue({ ok: false, message: 'Tunnel not found' }),
    };
    const res = await request(makeApp(pmd)).post('/location').send({ lat: 0, lng: 0 });
    expect(res.status).toBe(500);
    expect(res.body.ok).toBe(false);
    expect(res.body.message).toContain('Tunnel not found');
  });

  test('POST /reset calls pmd.clearLocation', async () => {
    const pmd = {
      clearLocation: jest.fn().mockResolvedValue({ ok: true, message: 'cleared' }),
    };
    const res = await request(makeApp(pmd)).post('/reset').send();
    expect(res.status).toBe(200);
    expect(res.body.ok).toBe(true);
    expect(pmd.clearLocation).toHaveBeenCalled();
  });

  test('GET /status returns device status', async () => {
    const pmd = {
      getStatus: jest.fn().mockResolvedValue({ ok: true, online: true, message: '裝置已連線' }),
    };
    const res = await request(makeApp(pmd)).get('/status');
    expect(res.status).toBe(200);
    expect(res.body.data.online).toBe(true);
  });
});
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `cd server && npx jest tests/routes.test.js`
Expected: FAIL — "Cannot find module '../src/routes'"

- [ ] **Step 3: 寫最小實作**

Create `server/src/routes.js`:

```javascript
const express = require('express');
const { validateCoords } = require('./validate');

function createRoutes(pmd) {
  const router = express.Router();

  router.post('/location', async (req, res) => {
    const { lat, lng } = req.body || {};
    const v = validateCoords(lat, lng);
    if (!v.ok) {
      res.status(400).json({ ok: false, message: v.message });
      return;
    }
    const result = await pmd.setLocation(v.lat, v.lng);
    res.status(result.ok ? 200 : 500).json(result);
  });

  router.post('/reset', async (req, res) => {
    const result = await pmd.clearLocation();
    res.status(result.ok ? 200 : 500).json(result);
  });

  router.get('/status', async (req, res) => {
    const result = await pmd.getStatus();
    res.status(200).json({ ok: result.ok, message: result.message, data: { online: result.online } });
  });

  return router;
}

module.exports = { createRoutes };
```

- [ ] **Step 4: 跑測試確認通過**

Run: `cd server && npx jest tests/routes.test.js`
Expected: PASS（5 個測試全綠）

- [ ] **Step 5: Commit**

```bash
git add server/src/routes.js server/tests/routes.test.js
git commit -m "feat: add HTTP routes"
```

---

### Task 5: Server 進入點（index.js）

**Files:**
- Create: `server/src/index.js`

說明：組裝 express app、JSON middleware、CORS（讓實機 App 跨來源呼叫）、掛載真實 pmd 的路由，監聽 0.0.0.0:3000 讓區網裝置可連。此檔為 wiring，無單元測試；以手動 curl 驗證。

- [ ] **Step 1: 加入 cors 依賴**

Run: `cd server && npm install cors@^2.8.5`
Expected: `cors` 加入 dependencies。

- [ ] **Step 2: 寫實作**

Create `server/src/index.js`:

```javascript
const express = require('express');
const cors = require('cors');
const { createPmd } = require('./pmd');
const { createRoutes } = require('./routes');

const PORT = process.env.PORT || 3000;

const app = express();
app.use(cors());
app.use(express.json());
app.use(createRoutes(createPmd()));

app.listen(PORT, '0.0.0.0', () => {
  console.log(`Ghost-Pin server listening on http://0.0.0.0:${PORT}`);
});
```

- [ ] **Step 3: 啟動並手動驗證（需 Phase 0 的 tunneld 在跑）**

Run: `cd server && npm start`（另開終端機執行下列 curl）

```bash
curl -s http://localhost:3000/status
# Expected: {"ok":true,"message":"裝置已連線","data":{"online":true}}

curl -s -X POST http://localhost:3000/location -H 'Content-Type: application/json' -d '{"lat":25.0330,"lng":121.5654}'
# Expected: {"ok":true,"message":"..."}  → iPhone 地圖藍點到台北101

curl -s -X POST http://localhost:3000/reset
# Expected: {"ok":true,"message":"..."}  → 藍點恢復

curl -s -X POST http://localhost:3000/location -H 'Content-Type: application/json' -d '{"lat":999,"lng":0}'
# Expected: HTTP 400 {"ok":false,"message":"lat 超出範圍 [-90, 90]"}
```

- [ ] **Step 4: 跑完整測試套件**

Run: `cd server && npx jest`
Expected: 全部 PASS（validate + pmd + routes）

- [ ] **Step 5: Commit**

```bash
git add server/src/index.js server/package.json server/package-lock.json
git commit -m "feat: add server entrypoint with CORS"
```

---

## Phase 2 — Expo App

### Task 6: 初始化 Expo 專案

**Files:**
- Create: `app/`（Expo 產生）

- [ ] **Step 1: 建立 Expo 專案**

Run: `npx create-expo-app@latest app --template blank-typescript`
Expected: `app/` 目錄建立，含 `App.tsx`、`package.json`。

- [ ] **Step 2: 確認可啟動**

Run: `cd app && npx expo start` （啟動後按 Ctrl+C 結束；僅確認無錯）
Expected: Metro bundler 啟動、印出 QR code，無 error。

- [ ] **Step 3: Commit**

```bash
git add app
git commit -m "chore: init expo app"
```

> 註：`app/node_modules` 由 Expo 產生的 `.gitignore` 處理；根目錄 `.gitignore` 已含 `node_modules/`。

---

### Task 7: Server 設定與 API client

**Files:**
- Create: `app/src/config.ts`
- Create: `app/src/api.ts`

說明：`config.ts` 集中 Server base URL。`api.ts` 包裝三個 HTTP 呼叫，回傳統一型別。無自動化測試（Expo blank 模板不含測試框架）；以 Task 8 的手動驗證涵蓋。

- [ ] **Step 1: 寫 config**

Create `app/src/config.ts`:

```typescript
// 模擬器用 localhost；實機測試請改成 Mac 的區網 IP，例如 'http://192.168.1.20:3000'
export const SERVER_BASE_URL = 'http://localhost:3000';
```

- [ ] **Step 2: 寫 API client**

Create `app/src/api.ts`:

```typescript
import { SERVER_BASE_URL } from './config';

export type ApiResult = { ok: boolean; message: string; data?: { online?: boolean } };

async function post(path: string, body?: object): Promise<ApiResult> {
  try {
    const res = await fetch(`${SERVER_BASE_URL}${path}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: body ? JSON.stringify(body) : undefined,
    });
    return (await res.json()) as ApiResult;
  } catch (e) {
    return { ok: false, message: `連線失敗：${String(e)}` };
  }
}

export function setLocation(lat: number, lng: number): Promise<ApiResult> {
  return post('/location', { lat, lng });
}

export function resetLocation(): Promise<ApiResult> {
  return post('/reset');
}

export async function getStatus(): Promise<ApiResult> {
  try {
    const res = await fetch(`${SERVER_BASE_URL}/status`);
    return (await res.json()) as ApiResult;
  } catch (e) {
    return { ok: false, message: `連線失敗：${String(e)}` };
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add app/src/config.ts app/src/api.ts
git commit -m "feat: add app config and api client"
```

---

### Task 8: 主畫面 UI（App.tsx）

**Files:**
- Modify: `app/App.tsx`（覆寫 Expo 預設內容）
- Create: `app/presets.ts`

說明：把 `presets.json` 的資料以 TypeScript 形式內嵌進 App（Expo 打包 JSON 較麻煩，直接用 .ts）。UI 含：lat/lng 輸入框、預設清單按鈕、設定/恢復鈕、狀態列。

- [ ] **Step 1: 建立 app 端預設清單**

Create `app/presets.ts`:

```typescript
export type Preset = { name: string; lat: number; lng: number };

export const PRESETS: Preset[] = [
  { name: '台北101', lat: 25.0330, lng: 121.5654 },
  { name: '台北車站', lat: 25.0478, lng: 121.5170 },
  { name: '桃園機場', lat: 25.0797, lng: 121.2342 },
];
```

- [ ] **Step 2: 覆寫 App.tsx**

Replace entire contents of `app/App.tsx`:

```tsx
import { useEffect, useState } from 'react';
import {
  SafeAreaView, ScrollView, View, Text, TextInput, Pressable, StyleSheet,
} from 'react-native';
import { PRESETS } from './presets';
import { setLocation, resetLocation, getStatus } from './src/api';

export default function App() {
  const [lat, setLat] = useState('25.0330');
  const [lng, setLng] = useState('121.5654');
  const [status, setStatus] = useState('檢查中…');
  const [busy, setBusy] = useState(false);

  async function refreshStatus() {
    const r = await getStatus();
    if (!r.ok) setStatus(`離線：${r.message}`);
    else setStatus(r.data?.online ? '裝置已連線' : '無裝置連線');
  }

  useEffect(() => { refreshStatus(); }, []);

  async function onSet() {
    setBusy(true);
    const r = await setLocation(Number(lat), Number(lng));
    setStatus(r.ok ? `已設定 ${lat}, ${lng}` : `失敗：${r.message}`);
    setBusy(false);
  }

  async function onReset() {
    setBusy(true);
    const r = await resetLocation();
    setStatus(r.ok ? '已恢復真實定位' : `失敗：${r.message}`);
    setBusy(false);
  }

  return (
    <SafeAreaView style={styles.root}>
      <ScrollView contentContainerStyle={styles.container}>
        <Text style={styles.title}>Ghost-Pin</Text>
        <Text style={styles.status}>{status}</Text>

        <Text style={styles.label}>緯度 (lat)</Text>
        <TextInput style={styles.input} value={lat} onChangeText={setLat}
          keyboardType="numbers-and-punctuation" />
        <Text style={styles.label}>經度 (lng)</Text>
        <TextInput style={styles.input} value={lng} onChangeText={setLng}
          keyboardType="numbers-and-punctuation" />

        <Text style={styles.label}>預設地點</Text>
        <View style={styles.presets}>
          {PRESETS.map((p) => (
            <Pressable key={p.name} style={styles.preset}
              onPress={() => { setLat(String(p.lat)); setLng(String(p.lng)); }}>
              <Text style={styles.presetText}>{p.name}</Text>
            </Pressable>
          ))}
        </View>

        <Pressable style={[styles.btn, styles.primary]} disabled={busy} onPress={onSet}>
          <Text style={styles.btnText}>設定定位</Text>
        </Pressable>
        <Pressable style={[styles.btn, styles.secondary]} disabled={busy} onPress={onReset}>
          <Text style={styles.btnText}>恢復真實定位</Text>
        </Pressable>
        <Pressable style={styles.refresh} onPress={refreshStatus}>
          <Text style={styles.refreshText}>重新檢查狀態</Text>
        </Pressable>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  root: { flex: 1, backgroundColor: '#0f1419' },
  container: { padding: 24, gap: 8 },
  title: { fontSize: 28, fontWeight: '700', color: '#fff', marginBottom: 4 },
  status: { fontSize: 14, color: '#7fd1ff', marginBottom: 16 },
  label: { fontSize: 13, color: '#9aa', marginTop: 8 },
  input: {
    backgroundColor: '#1c2530', color: '#fff', borderRadius: 8,
    paddingHorizontal: 12, paddingVertical: 10, fontSize: 16,
  },
  presets: { flexDirection: 'row', flexWrap: 'wrap', gap: 8, marginVertical: 8 },
  preset: { backgroundColor: '#26323f', borderRadius: 16, paddingHorizontal: 14, paddingVertical: 8 },
  presetText: { color: '#cfe', fontSize: 14 },
  btn: { borderRadius: 10, paddingVertical: 14, alignItems: 'center', marginTop: 12 },
  primary: { backgroundColor: '#2563eb' },
  secondary: { backgroundColor: '#3a4654' },
  btnText: { color: '#fff', fontSize: 16, fontWeight: '600' },
  refresh: { alignItems: 'center', marginTop: 16 },
  refreshText: { color: '#7fd1ff', fontSize: 14 },
});
```

- [ ] **Step 3: 啟動 App 並手動驗證（Server + tunneld 需在跑）**

Run: `cd app && npx expo start`，在 iOS 模擬器或 Expo Go 開啟。

驗收：
1. 開啟後狀態列顯示「裝置已連線」（模擬器透過 localhost）。
2. 點「台北101」→ 輸入框填入 25.0330 / 121.5654。
3. 按「設定定位」→ 狀態列顯示「已設定…」，iPhone 地圖藍點移動。
4. 按「恢復真實定位」→ 狀態列顯示「已恢復真實定位」，藍點回原位。
5. 輸入 lat=999 按設定 → 狀態列顯示「失敗：lat 超出範圍 [-90, 90]」。

> 實機測試 App 時：把 `app/src/config.ts` 的 `SERVER_BASE_URL` 改成 Mac 區網 IP（`ipconfig getifaddr en0` 取得），且手機與 Mac 同一 Wi-Fi。

- [ ] **Step 4: Commit**

```bash
git add app/App.tsx app/presets.ts
git commit -m "feat: add main UI screen"
```

---

### Task 9: README 與收尾

**Files:**
- Create: `README.md`

- [ ] **Step 1: 寫 README**

Create `README.md`:

```markdown
# Ghost-Pin

改變實體 iPhone 定位的介面。Expo App → Node.js Server → pymobiledevice3 → iPhone（iOS 17+）。

## 一次性設定

1. `pipx install pymobiledevice3`
2. iPhone 開 Developer Mode、USB 連 Mac、信任電腦
3. `pymobiledevice3 mounter auto-mount`
4. 常駐 tunnel（保持開著）：`sudo pymobiledevice3 remote tunneld`

## 啟動

- Server：`cd server && npm install && npm start`（:3000）
- App：`cd app && npx expo start`

實機測試 App：把 `app/src/config.ts` 的 `SERVER_BASE_URL` 改成 Mac 區網 IP，手機與 Mac 同一 Wi-Fi。

## 測試

`cd server && npm test`

## 限制

- 僅支援單一 USB 連線的 iPhone、iOS 17+。
- 依賴 tunneld 常駐；Mac 重開或 tunnel 中斷需重跑。
- Server 不執行 sudo；tunneld 由使用者手動啟動。
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: add README"
```

---

## Self-Review

**Spec coverage:**
- 靜態座標設定 → Task 4 `/location` + Task 8 UI ✅
- 一鍵恢復 → Task 4 `/reset` + Task 8 恢復鈕 ✅
- iOS 17+ `developer dvt` 指令 → Task 3 pmd.js ✅
- tunneld 手動常駐、Server 不碰 sudo → Phase 0 Step 4 + Task 5（無 sudo）✅
- 三端點 API → Task 4 ✅
- 座標驗證與範圍 → Task 2 ✅
- 命令注入防護（execFile 參數陣列）→ Task 3 ✅
- 統一 JSON 回應格式 → Task 4 ✅
- 預設地點清單 → Task 1 presets.json + Task 8 presets.ts ✅
- Server IP 可設定 → Task 7 config.ts ✅
- 錯誤處理表（400/503/500/連線失敗）→ Task 2/3/4 + Task 7 catch ✅
- 狀態列顯示 tunnel/裝置在線 → Task 8 ✅
- Phase 0 spike 先行 → Phase 0 ✅
- 測試策略（validate 單元、pmd mock、routes supertest）→ Task 2/3/4 ✅

> 註：spec 錯誤處理表提到 `/location` 裝置離線回 503。實作上裝置離線時 pymobiledevice3 會執行失敗，由 Task 4 統一回 500 並帶 stderr 摘要。已簡化為單一錯誤路徑（500 帶明確訊息），不另做 pre-flight 503 檢查以符合 YAGNI；使用者仍可透過 `/status` 與狀態列得知離線。

**Placeholder scan:** 無 TBD/TODO；所有 code 步驟皆含完整程式碼與預期輸出。

**Type consistency:** `ApiResult`、`{ ok, message, data }` 跨 pmd.js / routes.js / api.ts 一致；`setLocation/clearLocation/getStatus` 命名跨 Server 與 App 對應一致。
